// Generator modules manifest.
'use strict';

console.log('[BitdogLab] Generator modules loaded.');

if (window.Code && window.Code.BlockTypeDomains) {
  window.Code.BlockTypeDomains.applySemanticConnectionModel();
}
